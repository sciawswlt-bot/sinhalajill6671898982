// GitHub Pages project sites are served under /<repo-name>/, not the
// domain root, so both the slug-extraction logic (app.js) and the
// generated-link builder (admin.js) need to know this prefix.
//
// If you rename the repo, update this value to match.
const SITE_BASE_PATH = '/sinhalajill6671898982';
